local plr=game.Players.LocalPlayer
local rs=game:GetService("ReplicatedStorage")
local net=rs:WaitForChild("Modules"):WaitForChild("Net")
local reghit=net:WaitForChild("RE/RegisterHit")
local regatk=net:WaitForChild("RE/RegisterAttack")
local function gettargets()
	local char=plr.Character
	if not char then return {} end
	local hrp=char:FindFirstChild("HumanoidRootPart")
	if not hrp then return {} end
	local t={}
	for _,v in pairs(workspace:GetDescendants()) do
		if v:IsA("Model") then
			local h=v:FindFirstChildOfClass("Humanoid")
			if h and h.Health>0 and not game.Players:GetPlayerFromCharacter(v) then
				local part=v:FindFirstChild("HumanoidRootPart") or v:FindFirstChildWhichIsA("BasePart")
				if part then
					local d=(part.Position-hrp.Position).Magnitude
					if d<60 then
						table.insert(t,part)
					end
				end
			end
		end
	end
	return t
end
task.spawn(function()
	while true do
		local char=plr.Character
		local hasTool=false
		if char then
			for _,v in pairs(char:GetChildren()) do
				if v:IsA("Tool") then
					hasTool=true
					break
				end
			end
		end
		if hasTool then
			local targets=gettargets()
			if #targets>0 then
				regatk:FireServer(0.5)
				for i=1,#targets do
					reghit:FireServer(targets[i],{}, "5031e87d")
					task.wait(0.005)
				end
			end
		end
		task.wait(0.05)
	end
end)
